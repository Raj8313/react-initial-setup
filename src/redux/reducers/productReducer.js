const initialState = {
  loading: false,
  error: false,
  success: false,
  errorMessage: false,
  data: {},
};

const productReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productReducer;
